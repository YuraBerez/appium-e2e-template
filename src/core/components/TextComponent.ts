import BaseTextComponent from '../base/BaseTextComponent';
/**
 * Represents a standard, simple text element in the application UI.
 */
export default class TextComponent extends BaseTextComponent {
    
    constructor(selector: string) {
        super(selector);
    }
}